{
  "scripts": {
    "dev": "next",
    "build": "NODE_ENV=production next build && next export && cd ../ && rm -r _next && rm -r index && mv __src/out/* ./ && git add . && git commit -m 'update' && git push"
  },
  "dependencies": {
    "axios": "^0.19.0",
    "next": "^8.1.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-markdown": "^4.0.8",
    "react-styled-clickable-progress-bar": "^1.0.2",
    "react-syntax-highlighter": "^10.3.0",
    "rebass": "^3.1.1",
    "styled-components": "^4.3.2"
  }
}
