{
  "scripts": {
    "dev": "next",
    "build": "next build && next export && cd ../ && rm -r _next && rm -r index && mv __src/out/* ./ && git add . && git commit -m 'update' && git push"
  },
  "dependencies": {
    "next": "^8.1.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-styled-clickable-progress-bar": "^1.0.2"
  }
}
